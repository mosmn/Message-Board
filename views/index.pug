extends layout

block content
  h1= title
  ul
    each message in messages
      li
        p= message.text
        p= "User: " + message.user
        p= "Added: " + message.added
  a(href="/new") Add new message
